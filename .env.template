# rejiglabs.com — names only, NEVER values. Real values live in Vercel env vars on the
# `website` project (and in RT's own .env locally). Site chat (#734) reads these.

# GoHighLevel, Rejig's OWN sub-account (not a client's)
GHL_REJIG_API_KEY=
GHL_REJIG_LOCATION_ID=
GHL_REJIG_DISCOVERY_CAL=

# LLM
OPENROUTER_API_KEY=
HELICONE_API_KEY=          # optional: observability only, the brain runs without it

# Escalation / Takeover ping to Rut
REJIG_SITECHAT_TELEGRAM_BOT_TOKEN=
REJIG_SITECHAT_TELEGRAM_CHAT_ID=

# Signs the widget's session so a browser cannot post as another visitor
SITE_CHAT_SIGNING_SECRET=

# Shared secret the IG/FB workflow sends as `x-webhook-secret` on its Webhook action (#741)
DM_WEBHOOK_SECRET=
